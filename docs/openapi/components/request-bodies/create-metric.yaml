CreateMetricRequestBody:
  description: Request body for creating a new metric.
  required: true
  content:
    application/json:
      schema:
        $ref: ../schemas/metric/metric-create-request.yaml#/MetricCreateRequest
