CreateTeamRequest:
  required: true
  description: Request body for creating a new team.
  content:
    application/json:
      schema:
        $ref: ../schemas/team/team-create-request.yaml#/TeamCreateRequest
